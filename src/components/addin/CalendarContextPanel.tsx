'use client';

export default function CalendarContextPanel() {
  return (
    <div className="p-4 text-sm">
      <h2 className="font-semibold">Calendar item</h2>
      <p className="mt-2 text-xs text-gray-500">
        AI-powered context for calendar events is coming in Phase 8 — attendee history, related
        conversations, and meeting preparation.
      </p>
      <p className="mt-3 text-xs text-gray-400">
        For now, this pane confirms it can appear alongside calendar view.
      </p>
    </div>
  );
}
